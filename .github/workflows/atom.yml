name: blank

on:
  push:

jobs:
  hesitate:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        tutor: ["peak", "civilize", "drama", "mayor", "shiver"]
        simplicity: ["lag", "bore", "butter", "lick"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar eq
